language: csharp
solution: LdapManager.sln

dotnet: 2.1
mono: none

install:
  - dotnet restore LdapManager.sln
script:
  - dotnet build 
  - dotnet test /p:CollectCoverage=true /p:CoverletOutputFormat=opencover
after_success: 
  - ./tools/csmacnz.Coveralls --opencover -i ./LdapManager.Test/coverage.opencover.xml --repoToken "$coverallToken"